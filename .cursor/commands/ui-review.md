Este projeto **não possui UI web**. 

Se o pedido for sobre notebooks Databricks ou documentação visual da entrega, use `/docs-sync` ou `/mvp-entrega` com o escopo: $ARGUMENTS

Caso contrário, informe que `/ui-review` não se aplica e sugira o comando adequado (`/pipeline-quality`, `/db-analysis`, `/security-audit`).
